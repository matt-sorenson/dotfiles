#!/usr/bin/env zsh
#compdef aws-signon

function() {
    emulate -L zsh
    set -uo pipefail

    local _usage="Usage: aws-signon [-f|--force] [-h|--help] <profile>

Options:
  -f, --force                         Force login even if already signed in
  -h, --help                          Show this help message
  <profile>                           AWS profile to sign into

Examples:
aws-signon -f dev
aws-signon dev
aws-signon dev -f
"

    local force=0
    local profile=""

    while (( $# )); do
        case "$1" in
            -h|--help)
                printf "%s" "${_usage}"
                return 0
                ;;
            -f|--force)
                force=1
                ;;
            -*)
                print-header red "❌ Unknown option: $1"
                printf "%s" "${_usage}"
                return 1
                ;;
            *)
                if [ -n "$profile" ]; then
                    print-header red "❌ Profile passed in multiple times!"
                    printf "%s" "${_usage}"
                    return 1
                fi
                export AWS_PROFILE="$1"
                ;;
        esac
        shift
    done

    export AWS_PROFILE
    if [ -z "$profile" ]; then
        if [ -z "$AWS_PROFILE" ]; then
            print-header red "❌ No profile specified & \$AWS_PROFILE is not available!"
            printf "%s" "${_usage}"
            return 1
        else
            printf "No profile specified, using \$AWS_PROFILE: %s\n" "$AWS_PROFILE"
        fi
    else
        AWS_PROFILE="$profile"
        unset profile
    fi

    print-header green "🔐 Signing into AWS: $AWS_PROFILE"

    # Set to 1 so that if force is set it will attempt to login
    local exit_code=1
    if (( ! force )); then
        aws sts get-caller-identity --profile "$AWS_PROFILE" &> /dev/null
        local exit_code=$?
    fi

    if (( exit_code != 0 )); then
        if (( ! force )); then
            echo "not signed in, attempting SSO login for '$AWS_PROFILE'..."
        fi
        aws sso login --profile "$AWS_PROFILE"
    else
        echo "already signed in as $AWS_PROFILE"
    fi
} "$@"
