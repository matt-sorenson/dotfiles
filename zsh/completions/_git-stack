#compdef git-stack
#description show your current git branch stack

_git-stack() {
    _arguments \
        '(-c, --clean)'{-c,--clean}'[Clear the branch stack]' \
        '(-h --help)'{-h,--help}'[Show help]'
}

_git-stack "$@"
